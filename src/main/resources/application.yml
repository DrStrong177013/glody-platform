spring:
  application:
    name: glody-platform

  mvc:
    view:
      prefix: classpath:/templates/
      suffix: .html

  thymeleaf:
    enabled: true
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
    cache: false

  datasource:
    url: ${MYSQL_URL:jdbc:mysql://localhost:3306/glody_db?useSSL=false&serverTimezone=UTC}
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  jackson:
    serialization:
      write-dates-as-timestamps: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: manhtdse173612@fpt.edu.vn
    password: cvymcpimkwjloiup
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
servers:
  - url: http://localhost:9876/api
    description: Local Dev Server
  - url: https://pog.threemusketeer.click/api
    description: Production Server

springdoc:
  swagger-ui:
    tagsSorter: alpha
    operationsSorter: none

flyway:
  baseline-on-migrate: true
  locations: classpath:db/migration

jwt:
  secret: secret-key-change-this
  expirationMs: 86400000

payos:
  client-id:    ${PAYOS_CLIENT_ID:757c90c4-63da-4d1f-8d70-041bee93be81}
  api-key:      ${PAYOS_API_KEY:0f7d0e96-672c-4698-9337-a122daf6aa1f}
  checksum-key: ${PAYOS_CHECKSUM_KEY:cc085211e7d008940e43cd45e807751c929fc1b34c403a396e037dbc56a28669}
  partner-code: ""
  base-url:     https://api-merchant.payos.vn
server:
  port: 9876
